rem ---------------------------------------------------------------
rem Build it, free it, free it again, and read the error out.
rem
rem Generated by tests/gen_lifecycle_suite.py -- edit that, not this.
rem
rem free, error, errormsg, strerror and clearerror were a quarter of the
rem GUI surface and none of them was called by any test. They are also
rem the quarter that fails silently: 25 *_free functions answered 0 on
rem every call for months, with the line that set the result commented
rem out, and nothing noticed because nothing looked at what came back.
rem
rem The second free is the point. HandleRegistry exists so a pointer kept
rem after freeing is detected rather than followed, and this asks it 97
rem times.
rem ---------------------------------------------------------------

f# = form#()
host# = rectangle#(f#)

test_case("lifecycle/affine")
affine_clearerror()
c# = affine#(host#)
assert_eq(affine_error(), 0, "affine# built with no error")
r = affine_free(c#)
assert_eq(r, 1, "affine_free answers 1")
assert_eq(affine_error(), 0, "affine_free records no error")
r = affine_free(c#)
assert_eq(r, 0, "a second affine_free is refused")
e = affine_error()
assert_true(e, "and the refusal is recorded")
s$ = affine_errormsg$()
assert_true(len(s$), "affine_errormsg$ says something")
s$ = affine_strerror$(e)
assert_true(len(s$), "affine_strerror$ names the code")
affine_clearerror()
assert_eq(affine_error(), 0, "affine_clearerror clears it")

test_case("lifecycle/arc")
arc_clearerror()
c# = arc#(f#)
assert_eq(arc_error(), 0, "arc# built with no error")
r = arc_free(c#)
assert_eq(r, 1, "arc_free answers 1")
assert_eq(arc_error(), 0, "arc_free records no error")
r = arc_free(c#)
assert_eq(r, 0, "a second arc_free is refused")
e = arc_error()
assert_true(e, "and the refusal is recorded")
s$ = arc_errormsg$()
assert_true(len(s$), "arc_errormsg$ says something")
s$ = arc_strerror$(e)
assert_true(len(s$), "arc_strerror$ names the code")
arc_clearerror()
assert_eq(arc_error(), 0, "arc_clearerror clears it")

test_case("lifecycle/bandedswirltr")
bandedswirltr_clearerror()
c# = bandedswirltr#(host#)
assert_eq(bandedswirltr_error(), 0, "bandedswirltr# built with no error")
r = bandedswirltr_free(c#)
assert_eq(r, 1, "bandedswirltr_free answers 1")
assert_eq(bandedswirltr_error(), 0, "bandedswirltr_free records no error")
r = bandedswirltr_free(c#)
assert_eq(r, 0, "a second bandedswirltr_free is refused")
e = bandedswirltr_error()
assert_true(e, "and the refusal is recorded")
s$ = bandedswirltr_errormsg$()
assert_true(len(s$), "bandedswirltr_errormsg$ says something")
s$ = bandedswirltr_strerror$(e)
assert_true(len(s$), "bandedswirltr_strerror$ names the code")
bandedswirltr_clearerror()
assert_eq(bandedswirltr_error(), 0, "bandedswirltr_clearerror clears it")

test_case("lifecycle/bands")
bands_clearerror()
c# = bands#(host#)
assert_eq(bands_error(), 0, "bands# built with no error")
r = bands_free(c#)
assert_eq(r, 1, "bands_free answers 1")
assert_eq(bands_error(), 0, "bands_free records no error")
r = bands_free(c#)
assert_eq(r, 0, "a second bands_free is refused")
e = bands_error()
assert_true(e, "and the refusal is recorded")
s$ = bands_errormsg$()
assert_true(len(s$), "bands_errormsg$ says something")
s$ = bands_strerror$(e)
assert_true(len(s$), "bands_strerror$ names the code")
bands_clearerror()
assert_eq(bands_error(), 0, "bands_clearerror clears it")

test_case("lifecycle/bevel")
bevel_clearerror()
c# = bevel#(host#)
assert_eq(bevel_error(), 0, "bevel# built with no error")
r = bevel_free(c#)
assert_eq(r, 1, "bevel_free answers 1")
assert_eq(bevel_error(), 0, "bevel_free records no error")
r = bevel_free(c#)
assert_eq(r, 0, "a second bevel_free is refused")
e = bevel_error()
assert_true(e, "and the refusal is recorded")
s$ = bevel_errormsg$()
assert_true(len(s$), "bevel_errormsg$ says something")
s$ = bevel_strerror$(e)
assert_true(len(s$), "bevel_strerror$ names the code")
bevel_clearerror()
assert_eq(bevel_error(), 0, "bevel_clearerror clears it")

test_case("lifecycle/blend")
blend_clearerror()
c# = blend#(host#)
assert_eq(blend_error(), 0, "blend# built with no error")
r = blend_free(c#)
assert_eq(r, 1, "blend_free answers 1")
assert_eq(blend_error(), 0, "blend_free records no error")
r = blend_free(c#)
assert_eq(r, 0, "a second blend_free is refused")
e = blend_error()
assert_true(e, "and the refusal is recorded")
s$ = blend_errormsg$()
assert_true(len(s$), "blend_errormsg$ says something")
s$ = blend_strerror$(e)
assert_true(len(s$), "blend_strerror$ names the code")
blend_clearerror()
assert_eq(blend_error(), 0, "blend_clearerror clears it")

test_case("lifecycle/blindtrans")
blindtrans_clearerror()
c# = blindtrans#(host#)
assert_eq(blindtrans_error(), 0, "blindtrans# built with no error")
r = blindtrans_free(c#)
assert_eq(r, 1, "blindtrans_free answers 1")
assert_eq(blindtrans_error(), 0, "blindtrans_free records no error")
r = blindtrans_free(c#)
assert_eq(r, 0, "a second blindtrans_free is refused")
e = blindtrans_error()
assert_true(e, "and the refusal is recorded")
s$ = blindtrans_errormsg$()
assert_true(len(s$), "blindtrans_errormsg$ says something")
s$ = blindtrans_strerror$(e)
assert_true(len(s$), "blindtrans_strerror$ names the code")
blindtrans_clearerror()
assert_eq(blindtrans_error(), 0, "blindtrans_clearerror clears it")

test_case("lifecycle/bloodtrans")
bloodtrans_clearerror()
c# = bloodtrans#(host#)
assert_eq(bloodtrans_error(), 0, "bloodtrans# built with no error")
r = bloodtrans_free(c#)
assert_eq(r, 1, "bloodtrans_free answers 1")
assert_eq(bloodtrans_error(), 0, "bloodtrans_free records no error")
r = bloodtrans_free(c#)
assert_eq(r, 0, "a second bloodtrans_free is refused")
e = bloodtrans_error()
assert_true(e, "and the refusal is recorded")
s$ = bloodtrans_errormsg$()
assert_true(len(s$), "bloodtrans_errormsg$ says something")
s$ = bloodtrans_strerror$(e)
assert_true(len(s$), "bloodtrans_strerror$ names the code")
bloodtrans_clearerror()
assert_eq(bloodtrans_error(), 0, "bloodtrans_clearerror clears it")

test_case("lifecycle/bloom")
bloom_clearerror()
c# = bloom#(host#)
assert_eq(bloom_error(), 0, "bloom# built with no error")
r = bloom_free(c#)
assert_eq(r, 1, "bloom_free answers 1")
assert_eq(bloom_error(), 0, "bloom_free records no error")
r = bloom_free(c#)
assert_eq(r, 0, "a second bloom_free is refused")
e = bloom_error()
assert_true(e, "and the refusal is recorded")
s$ = bloom_errormsg$()
assert_true(len(s$), "bloom_errormsg$ says something")
s$ = bloom_strerror$(e)
assert_true(len(s$), "bloom_strerror$ names the code")
bloom_clearerror()
assert_eq(bloom_error(), 0, "bloom_clearerror clears it")

test_case("lifecycle/blur")
blur_clearerror()
c# = blur#(host#)
assert_eq(blur_error(), 0, "blur# built with no error")
r = blur_free(c#)
assert_eq(r, 1, "blur_free answers 1")
assert_eq(blur_error(), 0, "blur_free records no error")
r = blur_free(c#)
assert_eq(r, 0, "a second blur_free is refused")
e = blur_error()
assert_true(e, "and the refusal is recorded")
s$ = blur_errormsg$()
assert_true(len(s$), "blur_errormsg$ says something")
s$ = blur_strerror$(e)
assert_true(len(s$), "blur_strerror$ names the code")
blur_clearerror()
assert_eq(blur_error(), 0, "blur_clearerror clears it")

test_case("lifecycle/blurtrans")
blurtrans_clearerror()
c# = blurtrans#(host#)
assert_eq(blurtrans_error(), 0, "blurtrans# built with no error")
r = blurtrans_free(c#)
assert_eq(r, 1, "blurtrans_free answers 1")
assert_eq(blurtrans_error(), 0, "blurtrans_free records no error")
r = blurtrans_free(c#)
assert_eq(r, 0, "a second blurtrans_free is refused")
e = blurtrans_error()
assert_true(e, "and the refusal is recorded")
s$ = blurtrans_errormsg$()
assert_true(len(s$), "blurtrans_errormsg$ says something")
s$ = blurtrans_strerror$(e)
assert_true(len(s$), "blurtrans_strerror$ names the code")
blurtrans_clearerror()
assert_eq(blurtrans_error(), 0, "blurtrans_clearerror clears it")

test_case("lifecycle/bmplistani")
bmplistani_clearerror()
c# = bmplistani#(host#)
assert_eq(bmplistani_error(), 0, "bmplistani# built with no error")
r = bmplistani_free(c#)
assert_eq(r, 1, "bmplistani_free answers 1")
assert_eq(bmplistani_error(), 0, "bmplistani_free records no error")
r = bmplistani_free(c#)
assert_eq(r, 0, "a second bmplistani_free is refused")
e = bmplistani_error()
assert_true(e, "and the refusal is recorded")
s$ = bmplistani_errormsg$()
assert_true(len(s$), "bmplistani_errormsg$ says something")
s$ = bmplistani_strerror$(e)
assert_true(len(s$), "bmplistani_strerror$ names the code")
bmplistani_clearerror()
assert_eq(bmplistani_error(), 0, "bmplistani_clearerror clears it")

test_case("lifecycle/boxblur")
boxblur_clearerror()
c# = boxblur#(host#)
assert_eq(boxblur_error(), 0, "boxblur# built with no error")
r = boxblur_free(c#)
assert_eq(r, 1, "boxblur_free answers 1")
assert_eq(boxblur_error(), 0, "boxblur_free records no error")
r = boxblur_free(c#)
assert_eq(r, 0, "a second boxblur_free is refused")
e = boxblur_error()
assert_true(e, "and the refusal is recorded")
s$ = boxblur_errormsg$()
assert_true(len(s$), "boxblur_errormsg$ says something")
s$ = boxblur_strerror$(e)
assert_true(len(s$), "boxblur_strerror$ names the code")
boxblur_clearerror()
assert_eq(boxblur_error(), 0, "boxblur_clearerror clears it")

test_case("lifecycle/brighttrans")
brighttrans_clearerror()
c# = brighttrans#(host#)
assert_eq(brighttrans_error(), 0, "brighttrans# built with no error")
r = brighttrans_free(c#)
assert_eq(r, 1, "brighttrans_free answers 1")
assert_eq(brighttrans_error(), 0, "brighttrans_free records no error")
r = brighttrans_free(c#)
assert_eq(r, 0, "a second brighttrans_free is refused")
e = brighttrans_error()
assert_true(e, "and the refusal is recorded")
s$ = brighttrans_errormsg$()
assert_true(len(s$), "brighttrans_errormsg$ says something")
s$ = brighttrans_strerror$(e)
assert_true(len(s$), "brighttrans_strerror$ names the code")
brighttrans_clearerror()
assert_eq(brighttrans_error(), 0, "brighttrans_clearerror clears it")

test_case("lifecycle/bswirl")
bswirl_clearerror()
c# = bswirl#(host#)
assert_eq(bswirl_error(), 0, "bswirl# built with no error")
r = bswirl_free(c#)
assert_eq(r, 1, "bswirl_free answers 1")
assert_eq(bswirl_error(), 0, "bswirl_free records no error")
r = bswirl_free(c#)
assert_eq(r, 0, "a second bswirl_free is refused")
e = bswirl_error()
assert_true(e, "and the refusal is recorded")
s$ = bswirl_errormsg$()
assert_true(len(s$), "bswirl_errormsg$ says something")
s$ = bswirl_strerror$(e)
assert_true(len(s$), "bswirl_strerror$ names the code")
bswirl_clearerror()
assert_eq(bswirl_error(), 0, "bswirl_clearerror clears it")

test_case("lifecycle/button")
button_clearerror()
c# = button#(f#)
assert_eq(button_error(), 0, "button# built with no error")
r = button_free(c#)
assert_eq(r, 1, "button_free answers 1")
assert_eq(button_error(), 0, "button_free records no error")
r = button_free(c#)
assert_eq(r, 0, "a second button_free is refused")
e = button_error()
assert_true(e, "and the refusal is recorded")
s$ = button_errormsg$()
assert_true(len(s$), "button_errormsg$ says something")
s$ = button_strerror$(e)
assert_true(len(s$), "button_strerror$ names the code")
button_clearerror()
assert_eq(button_error(), 0, "button_clearerror clears it")

test_case("lifecycle/callout")
callout_clearerror()
c# = callout#(f#)
assert_eq(callout_error(), 0, "callout# built with no error")
r = callout_free(c#)
assert_eq(r, 1, "callout_free answers 1")
assert_eq(callout_error(), 0, "callout_free records no error")
r = callout_free(c#)
assert_eq(r, 0, "a second callout_free is refused")
e = callout_error()
assert_true(e, "and the refusal is recorded")
s$ = callout_errormsg$()
assert_true(len(s$), "callout_errormsg$ says something")
s$ = callout_strerror$(e)
assert_true(len(s$), "callout_strerror$ names the code")
callout_clearerror()
assert_eq(callout_error(), 0, "callout_clearerror clears it")

test_case("lifecycle/checkbox")
checkbox_clearerror()
c# = checkbox#(f#)
assert_eq(checkbox_error(), 0, "checkbox# built with no error")
r = checkbox_free(c#)
assert_eq(r, 1, "checkbox_free answers 1")
assert_eq(checkbox_error(), 0, "checkbox_free records no error")
r = checkbox_free(c#)
assert_eq(r, 0, "a second checkbox_free is refused")
e = checkbox_error()
assert_true(e, "and the refusal is recorded")
s$ = checkbox_errormsg$()
assert_true(len(s$), "checkbox_errormsg$ says something")
s$ = checkbox_strerror$(e)
assert_true(len(s$), "checkbox_strerror$ names the code")
checkbox_clearerror()
assert_eq(checkbox_error(), 0, "checkbox_clearerror clears it")

test_case("lifecycle/circle")
circle_clearerror()
c# = circle#(f#)
assert_eq(circle_error(), 0, "circle# built with no error")
r = circle_free(c#)
assert_eq(r, 1, "circle_free answers 1")
assert_eq(circle_error(), 0, "circle_free records no error")
r = circle_free(c#)
assert_eq(r, 0, "a second circle_free is refused")
e = circle_error()
assert_true(e, "and the refusal is recorded")
s$ = circle_errormsg$()
assert_true(len(s$), "circle_errormsg$ says something")
s$ = circle_strerror$(e)
assert_true(len(s$), "circle_strerror$ names the code")
circle_clearerror()
assert_eq(circle_error(), 0, "circle_clearerror clears it")

test_case("lifecycle/circletrans")
circletrans_clearerror()
c# = circletrans#(host#)
assert_eq(circletrans_error(), 0, "circletrans# built with no error")
r = circletrans_free(c#)
assert_eq(r, 1, "circletrans_free answers 1")
assert_eq(circletrans_error(), 0, "circletrans_free records no error")
r = circletrans_free(c#)
assert_eq(r, 0, "a second circletrans_free is refused")
e = circletrans_error()
assert_true(e, "and the refusal is recorded")
s$ = circletrans_errormsg$()
assert_true(len(s$), "circletrans_errormsg$ says something")
s$ = circletrans_strerror$(e)
assert_true(len(s$), "circletrans_strerror$ names the code")
circletrans_clearerror()
assert_eq(circletrans_error(), 0, "circletrans_clearerror clears it")

test_case("lifecycle/colorani")
colorani_clearerror()
c# = colorani#(host#)
assert_eq(colorani_error(), 0, "colorani# built with no error")
r = colorani_free(c#)
assert_eq(r, 1, "colorani_free answers 1")
assert_eq(colorani_error(), 0, "colorani_free records no error")
r = colorani_free(c#)
assert_eq(r, 0, "a second colorani_free is refused")
e = colorani_error()
assert_true(e, "and the refusal is recorded")
s$ = colorani_errormsg$()
assert_true(len(s$), "colorani_errormsg$ says something")
s$ = colorani_strerror$(e)
assert_true(len(s$), "colorani_strerror$ names the code")
colorani_clearerror()
assert_eq(colorani_error(), 0, "colorani_clearerror clears it")

test_case("lifecycle/colorkey")
colorkey_clearerror()
c# = colorkey#(host#)
assert_eq(colorkey_error(), 0, "colorkey# built with no error")
r = colorkey_free(c#)
assert_eq(r, 1, "colorkey_free answers 1")
assert_eq(colorkey_error(), 0, "colorkey_free records no error")
r = colorkey_free(c#)
assert_eq(r, 0, "a second colorkey_free is refused")
e = colorkey_error()
assert_true(e, "and the refusal is recorded")
s$ = colorkey_errormsg$()
assert_true(len(s$), "colorkey_errormsg$ says something")
s$ = colorkey_strerror$(e)
assert_true(len(s$), "colorkey_strerror$ names the code")
colorkey_clearerror()
assert_eq(colorkey_error(), 0, "colorkey_clearerror clears it")

test_case("lifecycle/combobox")
combobox_clearerror()
c# = combobox#(f#)
assert_eq(combobox_error(), 0, "combobox# built with no error")
r = combobox_free(c#)
assert_eq(r, 1, "combobox_free answers 1")
assert_eq(combobox_error(), 0, "combobox_free records no error")
r = combobox_free(c#)
assert_eq(r, 0, "a second combobox_free is refused")
e = combobox_error()
assert_true(e, "and the refusal is recorded")
s$ = combobox_errormsg$()
assert_true(len(s$), "combobox_errormsg$ says something")
s$ = combobox_strerror$(e)
assert_true(len(s$), "combobox_strerror$ names the code")
combobox_clearerror()
assert_eq(combobox_error(), 0, "combobox_clearerror clears it")

test_case("lifecycle/contrast")
contrast_clearerror()
c# = contrast#(host#)
assert_eq(contrast_error(), 0, "contrast# built with no error")
r = contrast_free(c#)
assert_eq(r, 1, "contrast_free answers 1")
assert_eq(contrast_error(), 0, "contrast_free records no error")
r = contrast_free(c#)
assert_eq(r, 0, "a second contrast_free is refused")
e = contrast_error()
assert_true(e, "and the refusal is recorded")
s$ = contrast_errormsg$()
assert_true(len(s$), "contrast_errormsg$ says something")
s$ = contrast_strerror$(e)
assert_true(len(s$), "contrast_strerror$ names the code")
contrast_clearerror()
assert_eq(contrast_error(), 0, "contrast_clearerror clears it")

test_case("lifecycle/crop")
crop_clearerror()
c# = crop#(host#)
assert_eq(crop_error(), 0, "crop# built with no error")
r = crop_free(c#)
assert_eq(r, 1, "crop_free answers 1")
assert_eq(crop_error(), 0, "crop_free records no error")
r = crop_free(c#)
assert_eq(r, 0, "a second crop_free is refused")
e = crop_error()
assert_true(e, "and the refusal is recorded")
s$ = crop_errormsg$()
assert_true(len(s$), "crop_errormsg$ says something")
s$ = crop_strerror$(e)
assert_true(len(s$), "crop_strerror$ names the code")
crop_clearerror()
assert_eq(crop_error(), 0, "crop_clearerror clears it")

test_case("lifecycle/crumpletrans")
crumpletrans_clearerror()
c# = crumpletrans#(host#)
assert_eq(crumpletrans_error(), 0, "crumpletrans# built with no error")
r = crumpletrans_free(c#)
assert_eq(r, 1, "crumpletrans_free answers 1")
assert_eq(crumpletrans_error(), 0, "crumpletrans_free records no error")
r = crumpletrans_free(c#)
assert_eq(r, 0, "a second crumpletrans_free is refused")
e = crumpletrans_error()
assert_true(e, "and the refusal is recorded")
s$ = crumpletrans_errormsg$()
assert_true(len(s$), "crumpletrans_errormsg$ says something")
s$ = crumpletrans_strerror$(e)
assert_true(len(s$), "crumpletrans_strerror$ names the code")
crumpletrans_clearerror()
assert_eq(crumpletrans_error(), 0, "crumpletrans_clearerror clears it")

test_case("lifecycle/dirblur")
dirblur_clearerror()
c# = dirblur#(host#)
assert_eq(dirblur_error(), 0, "dirblur# built with no error")
r = dirblur_free(c#)
assert_eq(r, 1, "dirblur_free answers 1")
assert_eq(dirblur_error(), 0, "dirblur_free records no error")
r = dirblur_free(c#)
assert_eq(r, 0, "a second dirblur_free is refused")
e = dirblur_error()
assert_true(e, "and the refusal is recorded")
s$ = dirblur_errormsg$()
assert_true(len(s$), "dirblur_errormsg$ says something")
s$ = dirblur_strerror$(e)
assert_true(len(s$), "dirblur_strerror$ names the code")
dirblur_clearerror()
assert_eq(dirblur_error(), 0, "dirblur_clearerror clears it")

test_case("lifecycle/dissolvetrans")
dissolvetrans_clearerror()
c# = dissolvetrans#(host#)
assert_eq(dissolvetrans_error(), 0, "dissolvetrans# built with no error")
r = dissolvetrans_free(c#)
assert_eq(r, 1, "dissolvetrans_free answers 1")
assert_eq(dissolvetrans_error(), 0, "dissolvetrans_free records no error")
r = dissolvetrans_free(c#)
assert_eq(r, 0, "a second dissolvetrans_free is refused")
e = dissolvetrans_error()
assert_true(e, "and the refusal is recorded")
s$ = dissolvetrans_errormsg$()
assert_true(len(s$), "dissolvetrans_errormsg$ says something")
s$ = dissolvetrans_strerror$(e)
assert_true(len(s$), "dissolvetrans_strerror$ names the code")
dissolvetrans_clearerror()
assert_eq(dissolvetrans_error(), 0, "dissolvetrans_clearerror clears it")

test_case("lifecycle/droptrans")
droptrans_clearerror()
c# = droptrans#(host#)
assert_eq(droptrans_error(), 0, "droptrans# built with no error")
r = droptrans_free(c#)
assert_eq(r, 1, "droptrans_free answers 1")
assert_eq(droptrans_error(), 0, "droptrans_free records no error")
r = droptrans_free(c#)
assert_eq(r, 0, "a second droptrans_free is refused")
e = droptrans_error()
assert_true(e, "and the refusal is recorded")
s$ = droptrans_errormsg$()
assert_true(len(s$), "droptrans_errormsg$ says something")
s$ = droptrans_strerror$(e)
assert_true(len(s$), "droptrans_strerror$ names the code")
droptrans_clearerror()
assert_eq(droptrans_error(), 0, "droptrans_clearerror clears it")

test_case("lifecycle/edit")
edit_clearerror()
c# = edit#(f#)
assert_eq(edit_error(), 0, "edit# built with no error")
r = edit_free(c#)
assert_eq(r, 1, "edit_free answers 1")
assert_eq(edit_error(), 0, "edit_free records no error")
r = edit_free(c#)
assert_eq(r, 0, "a second edit_free is refused")
e = edit_error()
assert_true(e, "and the refusal is recorded")
s$ = edit_errormsg$()
assert_true(len(s$), "edit_errormsg$ says something")
s$ = edit_strerror$(e)
assert_true(len(s$), "edit_strerror$ names the code")
edit_clearerror()
assert_eq(edit_error(), 0, "edit_clearerror clears it")

test_case("lifecycle/ellipse")
ellipse_clearerror()
c# = ellipse#(f#)
assert_eq(ellipse_error(), 0, "ellipse# built with no error")
r = ellipse_free(c#)
assert_eq(r, 1, "ellipse_free answers 1")
assert_eq(ellipse_error(), 0, "ellipse_free records no error")
r = ellipse_free(c#)
assert_eq(r, 0, "a second ellipse_free is refused")
e = ellipse_error()
assert_true(e, "and the refusal is recorded")
s$ = ellipse_errormsg$()
assert_true(len(s$), "ellipse_errormsg$ says something")
s$ = ellipse_strerror$(e)
assert_true(len(s$), "ellipse_strerror$ names the code")
ellipse_clearerror()
assert_eq(ellipse_error(), 0, "ellipse_clearerror clears it")

test_case("lifecycle/emboss")
emboss_clearerror()
c# = emboss#(host#)
assert_eq(emboss_error(), 0, "emboss# built with no error")
r = emboss_free(c#)
assert_eq(r, 1, "emboss_free answers 1")
assert_eq(emboss_error(), 0, "emboss_free records no error")
r = emboss_free(c#)
assert_eq(r, 0, "a second emboss_free is refused")
e = emboss_error()
assert_true(e, "and the refusal is recorded")
s$ = emboss_errormsg$()
assert_true(len(s$), "emboss_errormsg$ says something")
s$ = emboss_strerror$(e)
assert_true(len(s$), "emboss_strerror$ names the code")
emboss_clearerror()
assert_eq(emboss_error(), 0, "emboss_clearerror clears it")

test_case("lifecycle/fadetrans")
fadetrans_clearerror()
c# = fadetrans#(host#)
assert_eq(fadetrans_error(), 0, "fadetrans# built with no error")
r = fadetrans_free(c#)
assert_eq(r, 1, "fadetrans_free answers 1")
assert_eq(fadetrans_error(), 0, "fadetrans_free records no error")
r = fadetrans_free(c#)
assert_eq(r, 0, "a second fadetrans_free is refused")
e = fadetrans_error()
assert_true(e, "and the refusal is recorded")
s$ = fadetrans_errormsg$()
assert_true(len(s$), "fadetrans_errormsg$ says something")
s$ = fadetrans_strerror$(e)
assert_true(len(s$), "fadetrans_strerror$ names the code")
fadetrans_clearerror()
assert_eq(fadetrans_error(), 0, "fadetrans_clearerror clears it")

test_case("lifecycle/fill")
fill_clearerror()
c# = fill#(host#)
assert_eq(fill_error(), 0, "fill# built with no error")
r = fill_free(c#)
assert_eq(r, 1, "fill_free answers 1")
assert_eq(fill_error(), 0, "fill_free records no error")
r = fill_free(c#)
assert_eq(r, 0, "a second fill_free is refused")
e = fill_error()
assert_true(e, "and the refusal is recorded")
s$ = fill_errormsg$()
assert_true(len(s$), "fill_errormsg$ says something")
s$ = fill_strerror$(e)
assert_true(len(s$), "fill_strerror$ names the code")
fill_clearerror()
assert_eq(fill_error(), 0, "fill_clearerror clears it")

test_case("lifecycle/fillrgb")
fillrgb_clearerror()
c# = fillrgb#(host#)
assert_eq(fillrgb_error(), 0, "fillrgb# built with no error")
r = fillrgb_free(c#)
assert_eq(r, 1, "fillrgb_free answers 1")
assert_eq(fillrgb_error(), 0, "fillrgb_free records no error")
r = fillrgb_free(c#)
assert_eq(r, 0, "a second fillrgb_free is refused")
e = fillrgb_error()
assert_true(e, "and the refusal is recorded")
s$ = fillrgb_errormsg$()
assert_true(len(s$), "fillrgb_errormsg$ says something")
s$ = fillrgb_strerror$(e)
assert_true(len(s$), "fillrgb_strerror$ names the code")
fillrgb_clearerror()
assert_eq(fillrgb_error(), 0, "fillrgb_clearerror clears it")

test_case("lifecycle/floatani")
floatani_clearerror()
c# = floatani#(host#)
assert_eq(floatani_error(), 0, "floatani# built with no error")
r = floatani_free(c#)
assert_eq(r, 1, "floatani_free answers 1")
assert_eq(floatani_error(), 0, "floatani_free records no error")
r = floatani_free(c#)
assert_eq(r, 0, "a second floatani_free is refused")
e = floatani_error()
assert_true(e, "and the refusal is recorded")
s$ = floatani_errormsg$()
assert_true(len(s$), "floatani_errormsg$ says something")
s$ = floatani_strerror$(e)
assert_true(len(s$), "floatani_strerror$ names the code")
floatani_clearerror()
assert_eq(floatani_error(), 0, "floatani_clearerror clears it")

test_case("lifecycle/gaussblur")
gaussblur_clearerror()
c# = gaussblur#(host#)
assert_eq(gaussblur_error(), 0, "gaussblur# built with no error")
r = gaussblur_free(c#)
assert_eq(r, 1, "gaussblur_free answers 1")
assert_eq(gaussblur_error(), 0, "gaussblur_free records no error")
r = gaussblur_free(c#)
assert_eq(r, 0, "a second gaussblur_free is refused")
e = gaussblur_error()
assert_true(e, "and the refusal is recorded")
s$ = gaussblur_errormsg$()
assert_true(len(s$), "gaussblur_errormsg$ says something")
s$ = gaussblur_strerror$(e)
assert_true(len(s$), "gaussblur_strerror$ names the code")
gaussblur_clearerror()
assert_eq(gaussblur_error(), 0, "gaussblur_clearerror clears it")

test_case("lifecycle/gloom")
gloom_clearerror()
c# = gloom#(host#)
assert_eq(gloom_error(), 0, "gloom# built with no error")
r = gloom_free(c#)
assert_eq(r, 1, "gloom_free answers 1")
assert_eq(gloom_error(), 0, "gloom_free records no error")
r = gloom_free(c#)
assert_eq(r, 0, "a second gloom_free is refused")
e = gloom_error()
assert_true(e, "and the refusal is recorded")
s$ = gloom_errormsg$()
assert_true(len(s$), "gloom_errormsg$ says something")
s$ = gloom_strerror$(e)
assert_true(len(s$), "gloom_strerror$ names the code")
gloom_clearerror()
assert_eq(gloom_error(), 0, "gloom_clearerror clears it")

test_case("lifecycle/glow")
glow_clearerror()
c# = glow#(host#)
assert_eq(glow_error(), 0, "glow# built with no error")
r = glow_free(c#)
assert_eq(r, 1, "glow_free answers 1")
assert_eq(glow_error(), 0, "glow_free records no error")
r = glow_free(c#)
assert_eq(r, 0, "a second glow_free is refused")
e = glow_error()
assert_true(e, "and the refusal is recorded")
s$ = glow_errormsg$()
assert_true(len(s$), "glow_errormsg$ says something")
s$ = glow_strerror$(e)
assert_true(len(s$), "glow_strerror$ names the code")
glow_clearerror()
assert_eq(glow_error(), 0, "glow_clearerror clears it")

test_case("lifecycle/hueadjust")
hueadjust_clearerror()
c# = hueadjust#(host#)
assert_eq(hueadjust_error(), 0, "hueadjust# built with no error")
r = hueadjust_free(c#)
assert_eq(r, 1, "hueadjust_free answers 1")
assert_eq(hueadjust_error(), 0, "hueadjust_free records no error")
r = hueadjust_free(c#)
assert_eq(r, 0, "a second hueadjust_free is refused")
e = hueadjust_error()
assert_true(e, "and the refusal is recorded")
s$ = hueadjust_errormsg$()
assert_true(len(s$), "hueadjust_errormsg$ says something")
s$ = hueadjust_strerror$(e)
assert_true(len(s$), "hueadjust_strerror$ names the code")
hueadjust_clearerror()
assert_eq(hueadjust_error(), 0, "hueadjust_clearerror clears it")

test_case("lifecycle/image")
image_clearerror()
c# = image#(f#)
assert_eq(image_error(), 0, "image# built with no error")
r = image_free(c#)
assert_eq(r, 1, "image_free answers 1")
assert_eq(image_error(), 0, "image_free records no error")
r = image_free(c#)
assert_eq(r, 0, "a second image_free is refused")
e = image_error()
assert_true(e, "and the refusal is recorded")
s$ = image_errormsg$()
assert_true(len(s$), "image_errormsg$ says something")
s$ = image_strerror$(e)
assert_true(len(s$), "image_strerror$ names the code")
image_clearerror()
assert_eq(image_error(), 0, "image_clearerror clears it")

test_case("lifecycle/innerglow")
innerglow_clearerror()
c# = innerglow#(host#)
assert_eq(innerglow_error(), 0, "innerglow# built with no error")
r = innerglow_free(c#)
assert_eq(r, 1, "innerglow_free answers 1")
assert_eq(innerglow_error(), 0, "innerglow_free records no error")
r = innerglow_free(c#)
assert_eq(r, 0, "a second innerglow_free is refused")
e = innerglow_error()
assert_true(e, "and the refusal is recorded")
s$ = innerglow_errormsg$()
assert_true(len(s$), "innerglow_errormsg$ says something")
s$ = innerglow_strerror$(e)
assert_true(len(s$), "innerglow_strerror$ names the code")
innerglow_clearerror()
assert_eq(innerglow_error(), 0, "innerglow_clearerror clears it")

test_case("lifecycle/intani")
intani_clearerror()
c# = intani#(host#)
assert_eq(intani_error(), 0, "intani# built with no error")
r = intani_free(c#)
assert_eq(r, 1, "intani_free answers 1")
assert_eq(intani_error(), 0, "intani_free records no error")
r = intani_free(c#)
assert_eq(r, 0, "a second intani_free is refused")
e = intani_error()
assert_true(e, "and the refusal is recorded")
s$ = intani_errormsg$()
assert_true(len(s$), "intani_errormsg$ says something")
s$ = intani_strerror$(e)
assert_true(len(s$), "intani_strerror$ names the code")
intani_clearerror()
assert_eq(intani_error(), 0, "intani_clearerror clears it")

test_case("lifecycle/invert")
invert_clearerror()
c# = invert#(host#)
assert_eq(invert_error(), 0, "invert# built with no error")
r = invert_free(c#)
assert_eq(r, 1, "invert_free answers 1")
assert_eq(invert_error(), 0, "invert_free records no error")
r = invert_free(c#)
assert_eq(r, 0, "a second invert_free is refused")
e = invert_error()
assert_true(e, "and the refusal is recorded")
s$ = invert_errormsg$()
assert_true(len(s$), "invert_errormsg$ says something")
s$ = invert_strerror$(e)
assert_true(len(s$), "invert_strerror$ names the code")
invert_clearerror()
assert_eq(invert_error(), 0, "invert_clearerror clears it")

test_case("lifecycle/label")
label_clearerror()
c# = label#(f#)
assert_eq(label_error(), 0, "label# built with no error")
r = label_free(c#)
assert_eq(r, 1, "label_free answers 1")
assert_eq(label_error(), 0, "label_free records no error")
r = label_free(c#)
assert_eq(r, 0, "a second label_free is refused")
e = label_error()
assert_true(e, "and the refusal is recorded")
s$ = label_errormsg$()
assert_true(len(s$), "label_errormsg$ says something")
s$ = label_strerror$(e)
assert_true(len(s$), "label_strerror$ names the code")
label_clearerror()
assert_eq(label_error(), 0, "label_clearerror clears it")

test_case("lifecycle/layout")
layout_clearerror()
c# = layout#(f#)
assert_eq(layout_error(), 0, "layout# built with no error")
r = layout_free(c#)
assert_eq(r, 1, "layout_free answers 1")
assert_eq(layout_error(), 0, "layout_free records no error")
r = layout_free(c#)
assert_eq(r, 0, "a second layout_free is refused")
e = layout_error()
assert_true(e, "and the refusal is recorded")
s$ = layout_errormsg$()
assert_true(len(s$), "layout_errormsg$ says something")
s$ = layout_strerror$(e)
assert_true(len(s$), "layout_strerror$ names the code")
layout_clearerror()
assert_eq(layout_error(), 0, "layout_clearerror clears it")

test_case("lifecycle/line")
line_clearerror()
c# = line#(f#)
assert_eq(line_error(), 0, "line# built with no error")
r = line_free(c#)
assert_eq(r, 1, "line_free answers 1")
assert_eq(line_error(), 0, "line_free records no error")
r = line_free(c#)
assert_eq(r, 0, "a second line_free is refused")
e = line_error()
assert_true(e, "and the refusal is recorded")
s$ = line_errormsg$()
assert_true(len(s$), "line_errormsg$ says something")
s$ = line_strerror$(e)
assert_true(len(s$), "line_strerror$ names the code")
line_clearerror()
assert_eq(line_error(), 0, "line_clearerror clears it")

test_case("lifecycle/linetrans")
linetrans_clearerror()
c# = linetrans#(host#)
assert_eq(linetrans_error(), 0, "linetrans# built with no error")
r = linetrans_free(c#)
assert_eq(r, 1, "linetrans_free answers 1")
assert_eq(linetrans_error(), 0, "linetrans_free records no error")
r = linetrans_free(c#)
assert_eq(r, 0, "a second linetrans_free is refused")
e = linetrans_error()
assert_true(e, "and the refusal is recorded")
s$ = linetrans_errormsg$()
assert_true(len(s$), "linetrans_errormsg$ says something")
s$ = linetrans_strerror$(e)
assert_true(len(s$), "linetrans_strerror$ names the code")
linetrans_clearerror()
assert_eq(linetrans_error(), 0, "linetrans_clearerror clears it")

test_case("lifecycle/listbox")
listbox_clearerror()
c# = listbox#(f#)
assert_eq(listbox_error(), 0, "listbox# built with no error")
r = listbox_free(c#)
assert_eq(r, 1, "listbox_free answers 1")
assert_eq(listbox_error(), 0, "listbox_free records no error")
r = listbox_free(c#)
assert_eq(r, 0, "a second listbox_free is refused")
e = listbox_error()
assert_true(e, "and the refusal is recorded")
s$ = listbox_errormsg$()
assert_true(len(s$), "listbox_errormsg$ says something")
s$ = listbox_strerror$(e)
assert_true(len(s$), "listbox_strerror$ names the code")
listbox_clearerror()
assert_eq(listbox_error(), 0, "listbox_clearerror clears it")

test_case("lifecycle/magnify")
magnify_clearerror()
c# = magnify#(host#)
assert_eq(magnify_error(), 0, "magnify# built with no error")
r = magnify_free(c#)
assert_eq(r, 1, "magnify_free answers 1")
assert_eq(magnify_error(), 0, "magnify_free records no error")
r = magnify_free(c#)
assert_eq(r, 0, "a second magnify_free is refused")
e = magnify_error()
assert_true(e, "and the refusal is recorded")
s$ = magnify_errormsg$()
assert_true(len(s$), "magnify_errormsg$ says something")
s$ = magnify_strerror$(e)
assert_true(len(s$), "magnify_strerror$ names the code")
magnify_clearerror()
assert_eq(magnify_error(), 0, "magnify_clearerror clears it")

test_case("lifecycle/magnifytrans")
magnifytrans_clearerror()
c# = magnifytrans#(host#)
assert_eq(magnifytrans_error(), 0, "magnifytrans# built with no error")
r = magnifytrans_free(c#)
assert_eq(r, 1, "magnifytrans_free answers 1")
assert_eq(magnifytrans_error(), 0, "magnifytrans_free records no error")
r = magnifytrans_free(c#)
assert_eq(r, 0, "a second magnifytrans_free is refused")
e = magnifytrans_error()
assert_true(e, "and the refusal is recorded")
s$ = magnifytrans_errormsg$()
assert_true(len(s$), "magnifytrans_errormsg$ says something")
s$ = magnifytrans_strerror$(e)
assert_true(len(s$), "magnifytrans_strerror$ names the code")
magnifytrans_clearerror()
assert_eq(magnifytrans_error(), 0, "magnifytrans_clearerror clears it")

test_case("lifecycle/mask2a")
mask2a_clearerror()
c# = mask2a#(host#)
assert_eq(mask2a_error(), 0, "mask2a# built with no error")
r = mask2a_free(c#)
assert_eq(r, 1, "mask2a_free answers 1")
assert_eq(mask2a_error(), 0, "mask2a_free records no error")
r = mask2a_free(c#)
assert_eq(r, 0, "a second mask2a_free is refused")
e = mask2a_error()
assert_true(e, "and the refusal is recorded")
s$ = mask2a_errormsg$()
assert_true(len(s$), "mask2a_errormsg$ says something")
s$ = mask2a_strerror$(e)
assert_true(len(s$), "mask2a_strerror$ names the code")
mask2a_clearerror()
assert_eq(mask2a_error(), 0, "mask2a_clearerror clears it")

test_case("lifecycle/memo")
memo_clearerror()
c# = memo#(f#)
assert_eq(memo_error(), 0, "memo# built with no error")
r = memo_free(c#)
assert_eq(r, 1, "memo_free answers 1")
assert_eq(memo_error(), 0, "memo_free records no error")
r = memo_free(c#)
assert_eq(r, 0, "a second memo_free is refused")
e = memo_error()
assert_true(e, "and the refusal is recorded")
s$ = memo_errormsg$()
assert_true(len(s$), "memo_errormsg$ says something")
s$ = memo_strerror$(e)
assert_true(len(s$), "memo_strerror$ names the code")
memo_clearerror()
assert_eq(memo_error(), 0, "memo_clearerror clears it")

test_case("lifecycle/monochrome")
monochrome_clearerror()
c# = monochrome#(host#)
assert_eq(monochrome_error(), 0, "monochrome# built with no error")
r = monochrome_free(c#)
assert_eq(r, 1, "monochrome_free answers 1")
assert_eq(monochrome_error(), 0, "monochrome_free records no error")
r = monochrome_free(c#)
assert_eq(r, 0, "a second monochrome_free is refused")
e = monochrome_error()
assert_true(e, "and the refusal is recorded")
s$ = monochrome_errormsg$()
assert_true(len(s$), "monochrome_errormsg$ says something")
s$ = monochrome_strerror$(e)
assert_true(len(s$), "monochrome_strerror$ names the code")
monochrome_clearerror()
assert_eq(monochrome_error(), 0, "monochrome_clearerror clears it")

test_case("lifecycle/panel")
panel_clearerror()
c# = panel#(f#)
assert_eq(panel_error(), 0, "panel# built with no error")
r = panel_free(c#)
assert_eq(r, 1, "panel_free answers 1")
assert_eq(panel_error(), 0, "panel_free records no error")
r = panel_free(c#)
assert_eq(r, 0, "a second panel_free is refused")
e = panel_error()
assert_true(e, "and the refusal is recorded")
s$ = panel_errormsg$()
assert_true(len(s$), "panel_errormsg$ says something")
s$ = panel_strerror$(e)
assert_true(len(s$), "panel_strerror$ names the code")
panel_clearerror()
assert_eq(panel_error(), 0, "panel_clearerror clears it")

test_case("lifecycle/papersketch")
papersketch_clearerror()
c# = papersketch#(host#)
assert_eq(papersketch_error(), 0, "papersketch# built with no error")
r = papersketch_free(c#)
assert_eq(r, 1, "papersketch_free answers 1")
assert_eq(papersketch_error(), 0, "papersketch_free records no error")
r = papersketch_free(c#)
assert_eq(r, 0, "a second papersketch_free is refused")
e = papersketch_error()
assert_true(e, "and the refusal is recorded")
s$ = papersketch_errormsg$()
assert_true(len(s$), "papersketch_errormsg$ says something")
s$ = papersketch_strerror$(e)
assert_true(len(s$), "papersketch_strerror$ names the code")
papersketch_clearerror()
assert_eq(papersketch_error(), 0, "papersketch_clearerror clears it")

test_case("lifecycle/path")
path_clearerror()
c# = path#(f#)
assert_eq(path_error(), 0, "path# built with no error")
r = path_free(c#)
assert_eq(r, 1, "path_free answers 1")
assert_eq(path_error(), 0, "path_free records no error")
r = path_free(c#)
assert_eq(r, 0, "a second path_free is refused")
e = path_error()
assert_true(e, "and the refusal is recorded")
s$ = path_errormsg$()
assert_true(len(s$), "path_errormsg$ says something")
s$ = path_strerror$(e)
assert_true(len(s$), "path_strerror$ names the code")
path_clearerror()
assert_eq(path_error(), 0, "path_clearerror clears it")

test_case("lifecycle/pathani")
pathani_clearerror()
c# = pathani#(host#)
assert_eq(pathani_error(), 0, "pathani# built with no error")
r = pathani_free(c#)
assert_eq(r, 1, "pathani_free answers 1")
assert_eq(pathani_error(), 0, "pathani_free records no error")
r = pathani_free(c#)
assert_eq(r, 0, "a second pathani_free is refused")
e = pathani_error()
assert_true(e, "and the refusal is recorded")
s$ = pathani_errormsg$()
assert_true(len(s$), "pathani_errormsg$ says something")
s$ = pathani_strerror$(e)
assert_true(len(s$), "pathani_strerror$ names the code")
pathani_clearerror()
assert_eq(pathani_error(), 0, "pathani_clearerror clears it")

test_case("lifecycle/pencilstroke")
pencilstroke_clearerror()
c# = pencilstroke#(host#)
assert_eq(pencilstroke_error(), 0, "pencilstroke# built with no error")
r = pencilstroke_free(c#)
assert_eq(r, 1, "pencilstroke_free answers 1")
assert_eq(pencilstroke_error(), 0, "pencilstroke_free records no error")
r = pencilstroke_free(c#)
assert_eq(r, 0, "a second pencilstroke_free is refused")
e = pencilstroke_error()
assert_true(e, "and the refusal is recorded")
s$ = pencilstroke_errormsg$()
assert_true(len(s$), "pencilstroke_errormsg$ says something")
s$ = pencilstroke_strerror$(e)
assert_true(len(s$), "pencilstroke_strerror$ names the code")
pencilstroke_clearerror()
assert_eq(pencilstroke_error(), 0, "pencilstroke_clearerror clears it")

test_case("lifecycle/persp")
persp_clearerror()
c# = persp#(host#)
assert_eq(persp_error(), 0, "persp# built with no error")
r = persp_free(c#)
assert_eq(r, 1, "persp_free answers 1")
assert_eq(persp_error(), 0, "persp_free records no error")
r = persp_free(c#)
assert_eq(r, 0, "a second persp_free is refused")
e = persp_error()
assert_true(e, "and the refusal is recorded")
s$ = persp_errormsg$()
assert_true(len(s$), "persp_errormsg$ says something")
s$ = persp_strerror$(e)
assert_true(len(s$), "persp_strerror$ names the code")
persp_clearerror()
assert_eq(persp_error(), 0, "persp_clearerror clears it")

test_case("lifecycle/pie")
pie_clearerror()
c# = pie#(f#)
assert_eq(pie_error(), 0, "pie# built with no error")
r = pie_free(c#)
assert_eq(r, 1, "pie_free answers 1")
assert_eq(pie_error(), 0, "pie_free records no error")
r = pie_free(c#)
assert_eq(r, 0, "a second pie_free is refused")
e = pie_error()
assert_true(e, "and the refusal is recorded")
s$ = pie_errormsg$()
assert_true(len(s$), "pie_errormsg$ says something")
s$ = pie_strerror$(e)
assert_true(len(s$), "pie_strerror$ names the code")
pie_clearerror()
assert_eq(pie_error(), 0, "pie_clearerror clears it")

test_case("lifecycle/pinch")
pinch_clearerror()
c# = pinch#(host#)
assert_eq(pinch_error(), 0, "pinch# built with no error")
r = pinch_free(c#)
assert_eq(r, 1, "pinch_free answers 1")
assert_eq(pinch_error(), 0, "pinch_free records no error")
r = pinch_free(c#)
assert_eq(r, 0, "a second pinch_free is refused")
e = pinch_error()
assert_true(e, "and the refusal is recorded")
s$ = pinch_errormsg$()
assert_true(len(s$), "pinch_errormsg$ says something")
s$ = pinch_strerror$(e)
assert_true(len(s$), "pinch_strerror$ names the code")
pinch_clearerror()
assert_eq(pinch_error(), 0, "pinch_clearerror clears it")

test_case("lifecycle/pixelate")
pixelate_clearerror()
c# = pixelate#(host#)
assert_eq(pixelate_error(), 0, "pixelate# built with no error")
r = pixelate_free(c#)
assert_eq(r, 1, "pixelate_free answers 1")
assert_eq(pixelate_error(), 0, "pixelate_free records no error")
r = pixelate_free(c#)
assert_eq(r, 0, "a second pixelate_free is refused")
e = pixelate_error()
assert_true(e, "and the refusal is recorded")
s$ = pixelate_errormsg$()
assert_true(len(s$), "pixelate_errormsg$ says something")
s$ = pixelate_strerror$(e)
assert_true(len(s$), "pixelate_strerror$ names the code")
pixelate_clearerror()
assert_eq(pixelate_error(), 0, "pixelate_clearerror clears it")

test_case("lifecycle/pixelatetrans")
pixelatetrans_clearerror()
c# = pixelatetrans#(host#)
assert_eq(pixelatetrans_error(), 0, "pixelatetrans# built with no error")
r = pixelatetrans_free(c#)
assert_eq(r, 1, "pixelatetrans_free answers 1")
assert_eq(pixelatetrans_error(), 0, "pixelatetrans_free records no error")
r = pixelatetrans_free(c#)
assert_eq(r, 0, "a second pixelatetrans_free is refused")
e = pixelatetrans_error()
assert_true(e, "and the refusal is recorded")
s$ = pixelatetrans_errormsg$()
assert_true(len(s$), "pixelatetrans_errormsg$ says something")
s$ = pixelatetrans_strerror$(e)
assert_true(len(s$), "pixelatetrans_strerror$ names the code")
pixelatetrans_clearerror()
assert_eq(pixelatetrans_error(), 0, "pixelatetrans_clearerror clears it")

test_case("lifecycle/progressbar")
progressbar_clearerror()
c# = progressbar#(f#)
assert_eq(progressbar_error(), 0, "progressbar# built with no error")
r = progressbar_free(c#)
assert_eq(r, 1, "progressbar_free answers 1")
assert_eq(progressbar_error(), 0, "progressbar_free records no error")
r = progressbar_free(c#)
assert_eq(r, 0, "a second progressbar_free is refused")
e = progressbar_error()
assert_true(e, "and the refusal is recorded")
s$ = progressbar_errormsg$()
assert_true(len(s$), "progressbar_errormsg$ says something")
s$ = progressbar_strerror$(e)
assert_true(len(s$), "progressbar_strerror$ names the code")
progressbar_clearerror()
assert_eq(progressbar_error(), 0, "progressbar_clearerror clears it")

test_case("lifecycle/radblur")
radblur_clearerror()
c# = radblur#(host#)
assert_eq(radblur_error(), 0, "radblur# built with no error")
r = radblur_free(c#)
assert_eq(r, 1, "radblur_free answers 1")
assert_eq(radblur_error(), 0, "radblur_free records no error")
r = radblur_free(c#)
assert_eq(r, 0, "a second radblur_free is refused")
e = radblur_error()
assert_true(e, "and the refusal is recorded")
s$ = radblur_errormsg$()
assert_true(len(s$), "radblur_errormsg$ says something")
s$ = radblur_strerror$(e)
assert_true(len(s$), "radblur_strerror$ names the code")
radblur_clearerror()
assert_eq(radblur_error(), 0, "radblur_clearerror clears it")

test_case("lifecycle/radiobutton")
radiobutton_clearerror()
c# = radiobutton#(f#)
assert_eq(radiobutton_error(), 0, "radiobutton# built with no error")
r = radiobutton_free(c#)
assert_eq(r, 1, "radiobutton_free answers 1")
assert_eq(radiobutton_error(), 0, "radiobutton_free records no error")
r = radiobutton_free(c#)
assert_eq(r, 0, "a second radiobutton_free is refused")
e = radiobutton_error()
assert_true(e, "and the refusal is recorded")
s$ = radiobutton_errormsg$()
assert_true(len(s$), "radiobutton_errormsg$ says something")
s$ = radiobutton_strerror$(e)
assert_true(len(s$), "radiobutton_strerror$ names the code")
radiobutton_clearerror()
assert_eq(radiobutton_error(), 0, "radiobutton_clearerror clears it")

test_case("lifecycle/rectangle")
rectangle_clearerror()
c# = rectangle#(f#)
assert_eq(rectangle_error(), 0, "rectangle# built with no error")
r = rectangle_free(c#)
assert_eq(r, 1, "rectangle_free answers 1")
assert_eq(rectangle_error(), 0, "rectangle_free records no error")
r = rectangle_free(c#)
assert_eq(r, 0, "a second rectangle_free is refused")
e = rectangle_error()
assert_true(e, "and the refusal is recorded")
s$ = rectangle_errormsg$()
assert_true(len(s$), "rectangle_errormsg$ says something")
s$ = rectangle_strerror$(e)
assert_true(len(s$), "rectangle_strerror$ names the code")
rectangle_clearerror()
assert_eq(rectangle_error(), 0, "rectangle_clearerror clears it")

test_case("lifecycle/rectani")
rectani_clearerror()
c# = rectani#(host#)
assert_eq(rectani_error(), 0, "rectani# built with no error")
r = rectani_free(c#)
assert_eq(r, 1, "rectani_free answers 1")
assert_eq(rectani_error(), 0, "rectani_free records no error")
r = rectani_free(c#)
assert_eq(r, 0, "a second rectani_free is refused")
e = rectani_error()
assert_true(e, "and the refusal is recorded")
s$ = rectani_errormsg$()
assert_true(len(s$), "rectani_errormsg$ says something")
s$ = rectani_strerror$(e)
assert_true(len(s$), "rectani_strerror$ names the code")
rectani_clearerror()
assert_eq(rectani_error(), 0, "rectani_clearerror clears it")

test_case("lifecycle/reflection")
reflection_clearerror()
c# = reflection#(host#)
assert_eq(reflection_error(), 0, "reflection# built with no error")
r = reflection_free(c#)
assert_eq(r, 1, "reflection_free answers 1")
assert_eq(reflection_error(), 0, "reflection_free records no error")
r = reflection_free(c#)
assert_eq(r, 0, "a second reflection_free is refused")
e = reflection_error()
assert_true(e, "and the refusal is recorded")
s$ = reflection_errormsg$()
assert_true(len(s$), "reflection_errormsg$ says something")
s$ = reflection_strerror$(e)
assert_true(len(s$), "reflection_strerror$ names the code")
reflection_clearerror()
assert_eq(reflection_error(), 0, "reflection_clearerror clears it")

test_case("lifecycle/ripple")
ripple_clearerror()
c# = ripple#(host#)
assert_eq(ripple_error(), 0, "ripple# built with no error")
r = ripple_free(c#)
assert_eq(r, 1, "ripple_free answers 1")
assert_eq(ripple_error(), 0, "ripple_free records no error")
r = ripple_free(c#)
assert_eq(r, 0, "a second ripple_free is refused")
e = ripple_error()
assert_true(e, "and the refusal is recorded")
s$ = ripple_errormsg$()
assert_true(len(s$), "ripple_errormsg$ says something")
s$ = ripple_strerror$(e)
assert_true(len(s$), "ripple_strerror$ names the code")
ripple_clearerror()
assert_eq(ripple_error(), 0, "ripple_clearerror clears it")

test_case("lifecycle/rippletrans")
rippletrans_clearerror()
c# = rippletrans#(host#)
assert_eq(rippletrans_error(), 0, "rippletrans# built with no error")
r = rippletrans_free(c#)
assert_eq(r, 1, "rippletrans_free answers 1")
assert_eq(rippletrans_error(), 0, "rippletrans_free records no error")
r = rippletrans_free(c#)
assert_eq(r, 0, "a second rippletrans_free is refused")
e = rippletrans_error()
assert_true(e, "and the refusal is recorded")
s$ = rippletrans_errormsg$()
assert_true(len(s$), "rippletrans_errormsg$ says something")
s$ = rippletrans_strerror$(e)
assert_true(len(s$), "rippletrans_strerror$ names the code")
rippletrans_clearerror()
assert_eq(rippletrans_error(), 0, "rippletrans_clearerror clears it")

test_case("lifecycle/rotcrumpletrans")
rotcrumpletrans_clearerror()
c# = rotcrumpletrans#(host#)
assert_eq(rotcrumpletrans_error(), 0, "rotcrumpletrans# built with no error")
r = rotcrumpletrans_free(c#)
assert_eq(r, 1, "rotcrumpletrans_free answers 1")
assert_eq(rotcrumpletrans_error(), 0, "rotcrumpletrans_free records no error")
r = rotcrumpletrans_free(c#)
assert_eq(r, 0, "a second rotcrumpletrans_free is refused")
e = rotcrumpletrans_error()
assert_true(e, "and the refusal is recorded")
s$ = rotcrumpletrans_errormsg$()
assert_true(len(s$), "rotcrumpletrans_errormsg$ says something")
s$ = rotcrumpletrans_strerror$(e)
assert_true(len(s$), "rotcrumpletrans_strerror$ names the code")
rotcrumpletrans_clearerror()
assert_eq(rotcrumpletrans_error(), 0, "rotcrumpletrans_clearerror clears it")

test_case("lifecycle/roundrect")
roundrect_clearerror()
c# = roundrect#(f#)
assert_eq(roundrect_error(), 0, "roundrect# built with no error")
r = roundrect_free(c#)
assert_eq(r, 1, "roundrect_free answers 1")
assert_eq(roundrect_error(), 0, "roundrect_free records no error")
r = roundrect_free(c#)
assert_eq(r, 0, "a second roundrect_free is refused")
e = roundrect_error()
assert_true(e, "and the refusal is recorded")
s$ = roundrect_errormsg$()
assert_true(len(s$), "roundrect_errormsg$ says something")
s$ = roundrect_strerror$(e)
assert_true(len(s$), "roundrect_strerror$ names the code")
roundrect_clearerror()
assert_eq(roundrect_error(), 0, "roundrect_clearerror clears it")

test_case("lifecycle/saturatrans")
saturatrans_clearerror()
c# = saturatrans#(host#)
assert_eq(saturatrans_error(), 0, "saturatrans# built with no error")
r = saturatrans_free(c#)
assert_eq(r, 1, "saturatrans_free answers 1")
assert_eq(saturatrans_error(), 0, "saturatrans_free records no error")
r = saturatrans_free(c#)
assert_eq(r, 0, "a second saturatrans_free is refused")
e = saturatrans_error()
assert_true(e, "and the refusal is recorded")
s$ = saturatrans_errormsg$()
assert_true(len(s$), "saturatrans_errormsg$ says something")
s$ = saturatrans_strerror$(e)
assert_true(len(s$), "saturatrans_strerror$ names the code")
saturatrans_clearerror()
assert_eq(saturatrans_error(), 0, "saturatrans_clearerror clears it")

test_case("lifecycle/sepia")
sepia_clearerror()
c# = sepia#(host#)
assert_eq(sepia_error(), 0, "sepia# built with no error")
r = sepia_free(c#)
assert_eq(r, 1, "sepia_free answers 1")
assert_eq(sepia_error(), 0, "sepia_free records no error")
r = sepia_free(c#)
assert_eq(r, 0, "a second sepia_free is refused")
e = sepia_error()
assert_true(e, "and the refusal is recorded")
s$ = sepia_errormsg$()
assert_true(len(s$), "sepia_errormsg$ says something")
s$ = sepia_strerror$(e)
assert_true(len(s$), "sepia_strerror$ names the code")
sepia_clearerror()
assert_eq(sepia_error(), 0, "sepia_clearerror clears it")

test_case("lifecycle/shadow")
shadow_clearerror()
c# = shadow#(host#)
assert_eq(shadow_error(), 0, "shadow# built with no error")
r = shadow_free(c#)
assert_eq(r, 1, "shadow_free answers 1")
assert_eq(shadow_error(), 0, "shadow_free records no error")
r = shadow_free(c#)
assert_eq(r, 0, "a second shadow_free is refused")
e = shadow_error()
assert_true(e, "and the refusal is recorded")
s$ = shadow_errormsg$()
assert_true(len(s$), "shadow_errormsg$ says something")
s$ = shadow_strerror$(e)
assert_true(len(s$), "shadow_strerror$ names the code")
shadow_clearerror()
assert_eq(shadow_error(), 0, "shadow_clearerror clears it")

test_case("lifecycle/shapetrans")
shapetrans_clearerror()
c# = shapetrans#(host#)
assert_eq(shapetrans_error(), 0, "shapetrans# built with no error")
r = shapetrans_free(c#)
assert_eq(r, 1, "shapetrans_free answers 1")
assert_eq(shapetrans_error(), 0, "shapetrans_free records no error")
r = shapetrans_free(c#)
assert_eq(r, 0, "a second shapetrans_free is refused")
e = shapetrans_error()
assert_true(e, "and the refusal is recorded")
s$ = shapetrans_errormsg$()
assert_true(len(s$), "shapetrans_errormsg$ says something")
s$ = shapetrans_strerror$(e)
assert_true(len(s$), "shapetrans_strerror$ names the code")
shapetrans_clearerror()
assert_eq(shapetrans_error(), 0, "shapetrans_clearerror clears it")

test_case("lifecycle/sharpen")
sharpen_clearerror()
c# = sharpen#(host#)
assert_eq(sharpen_error(), 0, "sharpen# built with no error")
r = sharpen_free(c#)
assert_eq(r, 1, "sharpen_free answers 1")
assert_eq(sharpen_error(), 0, "sharpen_free records no error")
r = sharpen_free(c#)
assert_eq(r, 0, "a second sharpen_free is refused")
e = sharpen_error()
assert_true(e, "and the refusal is recorded")
s$ = sharpen_errormsg$()
assert_true(len(s$), "sharpen_errormsg$ says something")
s$ = sharpen_strerror$(e)
assert_true(len(s$), "sharpen_strerror$ names the code")
sharpen_clearerror()
assert_eq(sharpen_error(), 0, "sharpen_clearerror clears it")

test_case("lifecycle/slidetrans")
slidetrans_clearerror()
c# = slidetrans#(host#)
assert_eq(slidetrans_error(), 0, "slidetrans# built with no error")
r = slidetrans_free(c#)
assert_eq(r, 1, "slidetrans_free answers 1")
assert_eq(slidetrans_error(), 0, "slidetrans_free records no error")
r = slidetrans_free(c#)
assert_eq(r, 0, "a second slidetrans_free is refused")
e = slidetrans_error()
assert_true(e, "and the refusal is recorded")
s$ = slidetrans_errormsg$()
assert_true(len(s$), "slidetrans_errormsg$ says something")
s$ = slidetrans_strerror$(e)
assert_true(len(s$), "slidetrans_strerror$ names the code")
slidetrans_clearerror()
assert_eq(slidetrans_error(), 0, "slidetrans_clearerror clears it")

test_case("lifecycle/smag")
smag_clearerror()
c# = smag#(host#)
assert_eq(smag_error(), 0, "smag# built with no error")
r = smag_free(c#)
assert_eq(r, 1, "smag_free answers 1")
assert_eq(smag_error(), 0, "smag_free records no error")
r = smag_free(c#)
assert_eq(r, 0, "a second smag_free is refused")
e = smag_error()
assert_true(e, "and the refusal is recorded")
s$ = smag_errormsg$()
assert_true(len(s$), "smag_errormsg$ says something")
s$ = smag_strerror$(e)
assert_true(len(s$), "smag_strerror$ names the code")
smag_clearerror()
assert_eq(smag_error(), 0, "smag_clearerror clears it")

test_case("lifecycle/speedbutton")
speedbutton_clearerror()
c# = speedbutton#(f#)
assert_eq(speedbutton_error(), 0, "speedbutton# built with no error")
r = speedbutton_free(c#)
assert_eq(r, 1, "speedbutton_free answers 1")
assert_eq(speedbutton_error(), 0, "speedbutton_free records no error")
r = speedbutton_free(c#)
assert_eq(r, 0, "a second speedbutton_free is refused")
e = speedbutton_error()
assert_true(e, "and the refusal is recorded")
s$ = speedbutton_errormsg$()
assert_true(len(s$), "speedbutton_errormsg$ says something")
s$ = speedbutton_strerror$(e)
assert_true(len(s$), "speedbutton_strerror$ names the code")
speedbutton_clearerror()
assert_eq(speedbutton_error(), 0, "speedbutton_clearerror clears it")

test_case("lifecycle/stringgrid")
stringgrid_clearerror()
c# = stringgrid#(f#)
assert_eq(stringgrid_error(), 0, "stringgrid# built with no error")
r = stringgrid_free(c#)
assert_eq(r, 1, "stringgrid_free answers 1")
assert_eq(stringgrid_error(), 0, "stringgrid_free records no error")
r = stringgrid_free(c#)
assert_eq(r, 0, "a second stringgrid_free is refused")
e = stringgrid_error()
assert_true(e, "and the refusal is recorded")
s$ = stringgrid_errormsg$()
assert_true(len(s$), "stringgrid_errormsg$ says something")
s$ = stringgrid_strerror$(e)
assert_true(len(s$), "stringgrid_strerror$ names the code")
stringgrid_clearerror()
assert_eq(stringgrid_error(), 0, "stringgrid_clearerror clears it")

test_case("lifecycle/swipetrans")
swipetrans_clearerror()
c# = swipetrans#(host#)
assert_eq(swipetrans_error(), 0, "swipetrans# built with no error")
r = swipetrans_free(c#)
assert_eq(r, 1, "swipetrans_free answers 1")
assert_eq(swipetrans_error(), 0, "swipetrans_free records no error")
r = swipetrans_free(c#)
assert_eq(r, 0, "a second swipetrans_free is refused")
e = swipetrans_error()
assert_true(e, "and the refusal is recorded")
s$ = swipetrans_errormsg$()
assert_true(len(s$), "swipetrans_errormsg$ says something")
s$ = swipetrans_strerror$(e)
assert_true(len(s$), "swipetrans_strerror$ names the code")
swipetrans_clearerror()
assert_eq(swipetrans_error(), 0, "swipetrans_clearerror clears it")

test_case("lifecycle/swirl")
swirl_clearerror()
c# = swirl#(host#)
assert_eq(swirl_error(), 0, "swirl# built with no error")
r = swirl_free(c#)
assert_eq(r, 1, "swirl_free answers 1")
assert_eq(swirl_error(), 0, "swirl_free records no error")
r = swirl_free(c#)
assert_eq(r, 0, "a second swirl_free is refused")
e = swirl_error()
assert_true(e, "and the refusal is recorded")
s$ = swirl_errormsg$()
assert_true(len(s$), "swirl_errormsg$ says something")
s$ = swirl_strerror$(e)
assert_true(len(s$), "swirl_strerror$ names the code")
swirl_clearerror()
assert_eq(swirl_error(), 0, "swirl_clearerror clears it")

test_case("lifecycle/swirltrans")
swirltrans_clearerror()
c# = swirltrans#(host#)
assert_eq(swirltrans_error(), 0, "swirltrans# built with no error")
r = swirltrans_free(c#)
assert_eq(r, 1, "swirltrans_free answers 1")
assert_eq(swirltrans_error(), 0, "swirltrans_free records no error")
r = swirltrans_free(c#)
assert_eq(r, 0, "a second swirltrans_free is refused")
e = swirltrans_error()
assert_true(e, "and the refusal is recorded")
s$ = swirltrans_errormsg$()
assert_true(len(s$), "swirltrans_errormsg$ says something")
s$ = swirltrans_strerror$(e)
assert_true(len(s$), "swirltrans_strerror$ names the code")
swirltrans_clearerror()
assert_eq(swirltrans_error(), 0, "swirltrans_clearerror clears it")

test_case("lifecycle/switch")
switch_clearerror()
c# = switch#(f#)
assert_eq(switch_error(), 0, "switch# built with no error")
r = switch_free(c#)
assert_eq(r, 1, "switch_free answers 1")
assert_eq(switch_error(), 0, "switch_free records no error")
r = switch_free(c#)
assert_eq(r, 0, "a second switch_free is refused")
e = switch_error()
assert_true(e, "and the refusal is recorded")
s$ = switch_errormsg$()
assert_true(len(s$), "switch_errormsg$ says something")
s$ = switch_strerror$(e)
assert_true(len(s$), "switch_strerror$ names the code")
switch_clearerror()
assert_eq(switch_error(), 0, "switch_clearerror clears it")

test_case("lifecycle/tiler")
tiler_clearerror()
c# = tiler#(host#)
assert_eq(tiler_error(), 0, "tiler# built with no error")
r = tiler_free(c#)
assert_eq(r, 1, "tiler_free answers 1")
assert_eq(tiler_error(), 0, "tiler_free records no error")
r = tiler_free(c#)
assert_eq(r, 0, "a second tiler_free is refused")
e = tiler_error()
assert_true(e, "and the refusal is recorded")
s$ = tiler_errormsg$()
assert_true(len(s$), "tiler_errormsg$ says something")
s$ = tiler_strerror$(e)
assert_true(len(s$), "tiler_strerror$ names the code")
tiler_clearerror()
assert_eq(tiler_error(), 0, "tiler_clearerror clears it")

test_case("lifecycle/toon")
toon_clearerror()
c# = toon#(host#)
assert_eq(toon_error(), 0, "toon# built with no error")
r = toon_free(c#)
assert_eq(r, 1, "toon_free answers 1")
assert_eq(toon_error(), 0, "toon_free records no error")
r = toon_free(c#)
assert_eq(r, 0, "a second toon_free is refused")
e = toon_error()
assert_true(e, "and the refusal is recorded")
s$ = toon_errormsg$()
assert_true(len(s$), "toon_errormsg$ says something")
s$ = toon_strerror$(e)
assert_true(len(s$), "toon_strerror$ names the code")
toon_clearerror()
assert_eq(toon_error(), 0, "toon_clearerror clears it")

test_case("lifecycle/trackbar")
trackbar_clearerror()
c# = trackbar#(f#)
assert_eq(trackbar_error(), 0, "trackbar# built with no error")
r = trackbar_free(c#)
assert_eq(r, 1, "trackbar_free answers 1")
assert_eq(trackbar_error(), 0, "trackbar_free records no error")
r = trackbar_free(c#)
assert_eq(r, 0, "a second trackbar_free is refused")
e = trackbar_error()
assert_true(e, "and the refusal is recorded")
s$ = trackbar_errormsg$()
assert_true(len(s$), "trackbar_errormsg$ says something")
s$ = trackbar_strerror$(e)
assert_true(len(s$), "trackbar_strerror$ names the code")
trackbar_clearerror()
assert_eq(trackbar_error(), 0, "trackbar_clearerror clears it")

test_case("lifecycle/watertrans")
watertrans_clearerror()
c# = watertrans#(host#)
assert_eq(watertrans_error(), 0, "watertrans# built with no error")
r = watertrans_free(c#)
assert_eq(r, 1, "watertrans_free answers 1")
assert_eq(watertrans_error(), 0, "watertrans_free records no error")
r = watertrans_free(c#)
assert_eq(r, 0, "a second watertrans_free is refused")
e = watertrans_error()
assert_true(e, "and the refusal is recorded")
s$ = watertrans_errormsg$()
assert_true(len(s$), "watertrans_errormsg$ says something")
s$ = watertrans_strerror$(e)
assert_true(len(s$), "watertrans_strerror$ names the code")
watertrans_clearerror()
assert_eq(watertrans_error(), 0, "watertrans_clearerror clears it")

test_case("lifecycle/wave")
wave_clearerror()
c# = wave#(host#)
assert_eq(wave_error(), 0, "wave# built with no error")
r = wave_free(c#)
assert_eq(r, 1, "wave_free answers 1")
assert_eq(wave_error(), 0, "wave_free records no error")
r = wave_free(c#)
assert_eq(r, 0, "a second wave_free is refused")
e = wave_error()
assert_true(e, "and the refusal is recorded")
s$ = wave_errormsg$()
assert_true(len(s$), "wave_errormsg$ says something")
s$ = wave_strerror$(e)
assert_true(len(s$), "wave_strerror$ names the code")
wave_clearerror()
assert_eq(wave_error(), 0, "wave_clearerror clears it")

test_case("lifecycle/wavetrans")
wavetrans_clearerror()
c# = wavetrans#(host#)
assert_eq(wavetrans_error(), 0, "wavetrans# built with no error")
r = wavetrans_free(c#)
assert_eq(r, 1, "wavetrans_free answers 1")
assert_eq(wavetrans_error(), 0, "wavetrans_free records no error")
r = wavetrans_free(c#)
assert_eq(r, 0, "a second wavetrans_free is refused")
e = wavetrans_error()
assert_true(e, "and the refusal is recorded")
s$ = wavetrans_errormsg$()
assert_true(len(s$), "wavetrans_errormsg$ says something")
s$ = wavetrans_strerror$(e)
assert_true(len(s$), "wavetrans_strerror$ names the code")
wavetrans_clearerror()
assert_eq(wavetrans_error(), 0, "wavetrans_clearerror clears it")

test_case("lifecycle/wiggletrans")
wiggletrans_clearerror()
c# = wiggletrans#(host#)
assert_eq(wiggletrans_error(), 0, "wiggletrans# built with no error")
r = wiggletrans_free(c#)
assert_eq(r, 1, "wiggletrans_free answers 1")
assert_eq(wiggletrans_error(), 0, "wiggletrans_free records no error")
r = wiggletrans_free(c#)
assert_eq(r, 0, "a second wiggletrans_free is refused")
e = wiggletrans_error()
assert_true(e, "and the refusal is recorded")
s$ = wiggletrans_errormsg$()
assert_true(len(s$), "wiggletrans_errormsg$ says something")
s$ = wiggletrans_strerror$(e)
assert_true(len(s$), "wiggletrans_strerror$ names the code")
wiggletrans_clearerror()
assert_eq(wiggletrans_error(), 0, "wiggletrans_clearerror clears it")

test_case("lifecycle/wrap")
wrap_clearerror()
c# = wrap#(host#)
assert_eq(wrap_error(), 0, "wrap# built with no error")
r = wrap_free(c#)
assert_eq(r, 1, "wrap_free answers 1")
assert_eq(wrap_error(), 0, "wrap_free records no error")
r = wrap_free(c#)
assert_eq(r, 0, "a second wrap_free is refused")
e = wrap_error()
assert_true(e, "and the refusal is recorded")
s$ = wrap_errormsg$()
assert_true(len(s$), "wrap_errormsg$ says something")
s$ = wrap_strerror$(e)
assert_true(len(s$), "wrap_strerror$ names the code")
wrap_clearerror()
assert_eq(wrap_error(), 0, "wrap_clearerror clears it")
